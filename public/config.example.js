// Vul hier je eigen Google Maps API key in en hernoem dit bestand naar config.js.
// Belangrijk: beperk de key in Google Cloud Console tot je eigen domein via
// "HTTP referrers (websites)" — anders kan iedereen die de key vindt hem misbruiken.
window.LOOPLY_CONFIG = { googleMapsApiKey: "" };
